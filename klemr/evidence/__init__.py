"""Evidence assembly — Tier-1 evidence packet PDF (Step 5).

Splits claims by verified resolution, recomputes every total from rows, and emits
the packet (cover + three-gate test, summary table, one evidence page per filable
claim with the embedded Seller Center screenshot). Reproduces
``reference/build_packet.py``. Not implemented in this slice.
"""
