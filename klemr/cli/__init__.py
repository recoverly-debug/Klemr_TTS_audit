"""Minimal CLI mirroring the engine calls (Step 7).

A thin typer app (detect / verify / build-packet) for tests and automation. The
engine is the single source of truth; the CLI, like the UI, only orchestrates it.
Not implemented in this slice.
"""
